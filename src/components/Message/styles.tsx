import styled from "styled-components";

const fontSize: string = "0.8rem";

export const Message = styled.div`
  width: 100%;
  position: absolute;
  font-size: ${fontSize};
  font-weight: bold;
  margin: 12px 0;
`;
